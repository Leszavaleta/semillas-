<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>san andrea</title>

<style>
body{
    margin:0;
    padding:0;
    background:white;
    display:flex;
    justify-content:center;
    align-items:center;
    min-height:100vh;
}

img{
    max-width:100%;
    height:auto;
    display:block;
}
</style>

</head>
<body>

<img src="san andrea.png" alt="Infografía de la san andrea">

</body>
</html>

# Cargar librería
library(qrcode)

# URL de la página
url <- "https://leszavaleta.github.io/semillas-/andrea.html"

# Crear QR
qr <- qr_code(url)

# Guardar QR en alta calidad
png(
  filename = "QR_san andrea.png",
  width = 3000,
  height = 3000,
  res = 300
)

par(mar = c(0,0,0,0))
plot(qr)

dev.off()


# Cargar librería
library(qrcode)

# URL de la página
url <- "https://leszavaleta.github.io/semillas-/compost.html"

# Crear QR
qr <- qr_code(url)

# Guardar QR en alta calidad
png(
  filename = "QR_compost.png",
  width = 3000,
  height = 3000,
  res = 300
)

par(mar = c(0,0,0,0))
plot(qr)

dev.off()

cat("Código QR generado correctamente.")